<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sjq.edu.mapper.EduUploadMapper">

    <insert id="saveUpload">
        INSERT INTO edu_upload(id, gmt_create, gmt_modified, is_deleted, fname, alias, fullpath, ftype, order_id, fgroup, fpath)
        VALUES (#{id},#{gmtCreate},#{gmtModified},#{isDeleted},#{fname},#{alias},#{fullpath},#{ftype},#{orderId},#{fgroup},#{fpath})
    </insert>
</mapper>
